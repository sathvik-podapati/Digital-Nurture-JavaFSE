spring:
  application:
    name: product-service
  config:
    import: "optional:configserver:http://localhost:8888" # Spring Cloud Config Server integration

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/